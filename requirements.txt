# === Gestion des ventes de blocs de glace — dépendances ===
# Cœur
Django==5.2.4
django-environ==0.12.0          # configuration via variables d'environnement (.env)

# Base de données
psycopg2-binary==2.9.10         # driver PostgreSQL (repli SQLite automatique si absent)

# Formulaires / UI
django-crispy-forms==2.3        # rendu propre des formulaires
crispy-bootstrap5==2024.10      # template pack Bootstrap 5 pour crispy-forms
Pillow==11.0.0                  # ImageField (logos des référentiels)

# Exports
openpyxl==3.1.5                 # export Excel de l'historique
xhtml2pdf==0.2.16               # export PDF de l'historique (pur Python, compatible Windows)

# Production (optionnel)
gunicorn==23.0.0                # serveur WSGI de production
whitenoise==6.8.2               # service des fichiers statiques en production
